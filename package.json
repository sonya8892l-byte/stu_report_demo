{
  "name": "bounderless-learning-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "故宫研学学生端与教师端统一部署入口",
  "scripts": {
    "build": "npm --prefix 4-stu-learning ci --include=dev && npm --prefix 4-stu-learning run build && node scripts/build-vercel-site.mjs"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
